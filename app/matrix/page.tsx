'use client';

import {useEffect,useMemo,useState} from 'react';
import Link from 'next/link';
import {useAccount} from '@/components/AccountProvider';
import {hq} from '@/lib/hq/client';

const all=['ARI','ATL','BAL','BUF','CAR','CHI','CIN','CLE','DAL','DEN','DET','GB','HOU','IND','JAX','KC','LV','LAC','LAR','MIA','MIN','NE','NO','NYG','NYJ','PHI','PIT','SEA','SF','TB','TEN','WSH'];
const colors:Record<string,string>={BUF:'#1670d2',PHI:'#008a91',BAL:'#4730aa',GB:'#225e48',KC:'#e51b3e',TB:'#d50d2e'};
const division:Record<string,string>={BUF:'AFC-E',MIA:'AFC-E',NE:'AFC-E',NYJ:'AFC-E',BAL:'AFC-N',CIN:'AFC-N',CLE:'AFC-N',PIT:'AFC-N',HOU:'AFC-S',IND:'AFC-S',JAX:'AFC-S',TEN:'AFC-S',DEN:'AFC-W',KC:'AFC-W',LV:'AFC-W',LAC:'AFC-W',DAL:'NFC-E',NYG:'NFC-E',PHI:'NFC-E',WSH:'NFC-E',CHI:'NFC-N',DET:'NFC-N',GB:'NFC-N',MIN:'NFC-N',ATL:'NFC-S',CAR:'NFC-S',NO:'NFC-S',TB:'NFC-S',ARI:'NFC-W',LAR:'NFC-W',SEA:'NFC-W',SF:'NFC-W'};
const windows=[3,5,8,18] as const;
type Planned=Record<number,string>;type Location='all'|'home'|'away';type Availability='available'|'all'|'used'|'planned'|'allEntries';type GameType='all'|'division'|'nondivision'|'primetime'|'shortweek';type Market='all'|'favorite'|'underdog';
function oddsKey(away:string,home:string){return `${away}|${home}`}
export default function Matrix(){
 const {session,boot}=useAccount();const pools=(boot?.leagues||[]).filter((league:any)=>league.format==='survivor');
 const [id,setId]=useState('');const [data,setData]=useState<any>();const [entry,setEntry]=useState('');const [odds,setOdds]=useState<any[]>([]);const [start,setStart]=useState(boot?.meta?.week||1);const [length,setLength]=useState<(typeof windows)[number]>(5);const [location,setLocation]=useState<Location>('all');const [availability,setAvailability]=useState<Availability>('available');const [gameType,setGameType]=useState<GameType>('all');const [market,setMarket]=useState<Market>('all');const [minimum,setMinimum]=useState(0);const [advanced,setAdvanced]=useState(false);const [planned,setPlanned]=useState<Planned>({});
 useEffect(()=>{if(!id&&pools[0])setId(pools[0].id)},[pools.length,id]);useEffect(()=>{if(boot?.meta?.week&&start===1)setStart(boot.meta.week)},[boot?.meta?.week]);useEffect(()=>{if(id)hq({action:'matrix',leagueId:id}).then(setData)},[id]);useEffect(()=>{if(data&&(!entry||!data.entries.some((item:any)=>item.id===entry)))setEntry(data.entries[0]?.id||'')},[data,entry]);useEffect(()=>{if(data?.activeWeek)setStart(data.activeWeek)},[data?.activeWeek]);useEffect(()=>{fetch('/api/odds').then(response=>response.ok?response.json():null).then(result=>setOdds(result?.games||[])).catch(()=>undefined)},[]);useEffect(()=>{if(!entry)return;try{setPlanned(JSON.parse(localStorage.getItem(`pickemhq-plan-${id}-${entry}`)||'{}'))}catch{setPlanned({})}},[id,entry]);useEffect(()=>{if(entry)localStorage.setItem(`pickemhq-plan-${id}-${entry}`,JSON.stringify(planned))},[id,entry,planned]);
 if(!session)return <Link href="/login?next=/matrix" className="sports-action">Sign in to use the Survivor Matrix</Link>;
 const visible=Array.from({length:Math.min(length,19-start)},(_,index)=>start+index);const games=(Object.values(data?.schedules||{}) as any[][]).flat();const teamCodes=new Map<string,string>(games.flatMap((game:any)=>[[String(game.home.id),game.home.code],[String(game.away.id),game.away.code]]));const used=new Set((data?.picks||[]).filter((pick:any)=>pick.entryId===entry).map((pick:any)=>teamCodes.get(String(pick.teamId))).filter(Boolean));const usedAll=new Set((data?.picks||[]).map((pick:any)=>teamCodes.get(String(pick.teamId))).filter(Boolean));const byTeam=new Map<string,any[]>();games.forEach((game:any)=>[game.home.code,game.away.code].forEach((code:string)=>byTeam.set(code,[...(byTeam.get(code)||[]),game])));byTeam.forEach(list=>list.sort((a,b)=>Date.parse(a.kickoff)-Date.parse(b.kickoff)));const lineMap=new Map(odds.map((line:any)=>[oddsKey(line.away,line.home),Number(line.spread)]));
 const rows=useMemo(()=>all.map(code=>{const cells=visible.map(week=>{const game=data?.schedules?.[week]?.find((item:any)=>item.home.code===code||item.away.code===code);if(!game)return null;const away=game.home.code===code;const opponent=away?game.away.code:game.home.code;const teamGames=byTeam.get(code)||[];const index=teamGames.indexOf(game);const prior=index>0?teamGames[index-1]:null;const days=prior?(Date.parse(game.kickoff)-Date.parse(prior.kickoff))/86400000:null;const homeSpread=lineMap.get(oddsKey(game.away.name,game.home.name));const spread=Number.isFinite(homeSpread)?(away?-(homeSpread as number):homeSpread as number):null;return {week,opponent,away,divisional:division[code]===division[opponent],primetime:new Date(game.kickoff).getHours()>=20,shortWeek:days!==null&&days<7,spread};});const matches=(cell:any)=>{if(!cell)return false;if(location==='home'&&cell.away)return false;if(location==='away'&&!cell.away)return false;if(gameType==='division'&&!cell.divisional)return false;if(gameType==='nondivision'&&cell.divisional)return false;if(gameType==='primetime'&&!cell.primetime)return false;if(gameType==='shortweek'&&!cell.shortWeek)return false;if(market==='favorite'&&!(cell.spread!==null&&cell.spread<0))return false;if(market==='underdog'&&!(cell.spread!==null&&cell.spread>0))return false;if(minimum>0&&!(cell.spread!==null&&cell.spread<=-minimum))return false;return true;};const opportunities=cells.filter(matches).length;const isUsed=used.has(code);const isPlanned=Object.values(planned).includes(code);const availabilityMatch=availability==='all'||(availability==='available'&&!isUsed)||(availability==='used'&&isUsed)||(availability==='planned'&&isPlanned)||(availability==='allEntries'&&!usedAll.has(code));return {code,cells,opportunities,isUsed,match:availabilityMatch,matches};}),[data,visible,location,availability,gameType,market,minimum,entry,planned,odds]);
 const plan=(week:number,team:string)=>{if(used.has(team))return;setPlanned(current=>current[week]===team?Object.fromEntries(Object.entries(current).filter(([key])=>Number(key)!==week)):{...current,[week]:team});};const candidates=[...rows].filter(row=>!row.isUsed).sort((a,b)=>b.opportunities-a.opportunities).slice(0,4);const planCount=Object.keys(planned).length;
 return <div className="matrix-page"><header className="matrix-hero"><div><p className="score-label text-emerald-200">PICKEM HQ · SURVIVOR PLANNER</p><h1>Find your future windows</h1></div><p>Bright cells meet your filters. Everything else stays visible for context.</p></header><section className="matrix-toolbar" aria-label="Matrix setup"><select aria-label="Survivor league" value={id} onChange={event=>setId(event.target.value)}>{pools.map((pool:any)=><option key={pool.id} value={pool.id}>{pool.name}</option>)}</select><select aria-label="Survivor entry" value={entry} onChange={event=>setEntry(event.target.value)}>{data?.entries?.map((item:any)=><option key={item.id} value={item.id}>Entry {item.entryNumber||item.name}</option>)}</select><label>From<select value={start} onChange={event=>setStart(+event.target.value)}>{Array.from({length:18},(_,index)=>index+1).map(week=><option key={week} value={week}>W{week}</option>)}</select></label><label>Window<select value={length} onChange={event=>setLength(+event.target.value as (typeof windows)[number])}>{windows.map(window=><option key={window} value={window}>{window===18?'Season':`${window} weeks`}</option>)}</select></label></section><div className="matrix-lives"><strong>{data?.lives?.alive??0} of {data?.lives?.total??0} lives active</strong><span>Eliminated lives are hidden from planning.</span></div><section className="matrix-quick"><div className="matrix-chipset">{([['available','Unused'],['home','Home'],['away','Away']] as const).map(([value,label])=><button key={value} type="button" className={(value==='available'?availability===value:location===value)?'is-active':''} onClick={()=>value==='available'?setAvailability('available'):setLocation(value)}>{label}</button>)}</div><div className="matrix-chipset">{([['all','All games'],['favorite','Favorites']] as const).map(([value,label])=><button key={value} type="button" className={market===value?'is-active':''} onClick={()=>setMarket(value)}>{label}</button>)}</div><button type="button" className={`matrix-advanced ${advanced?'is-active':''}`} onClick={()=>setAdvanced(!advanced)}>Advanced filters {advanced?'−':'+'}</button></section>{advanced&&<section className="matrix-advanced-panel"><label>Availability<select value={availability} onChange={event=>setAvailability(event.target.value as Availability)}><option value="available">Unused in this entry</option><option value="allEntries">Open across all entries</option><option value="planned">Planned teams</option><option value="used">Used teams</option><option value="all">All teams</option></select></label><label>Schedule<select value={gameType} onChange={event=>setGameType(event.target.value as GameType)}><option value="all">Any game</option><option value="division">Divisional only</option><option value="nondivision">Non-divisional only</option><option value="primetime">Primetime only</option><option value="shortweek">Short week only</option></select></label><label>Market<select value={market} onChange={event=>setMarket(event.target.value as Market)}><option value="all">Any line</option><option value="favorite">Favorites only</option><option value="underdog">Underdogs only</option></select></label><label>Min favorite<select value={minimum} onChange={event=>setMinimum(+event.target.value)}><option value={0}>Any spread</option><option value={3}>-3 or better</option><option value={6}>-6 or better</option><option value={7}>-7 or better</option><option value={10}>-10 or better</option></select></label><p>Market filters use live lines when available. Future weeks stay visible even before look-ahead lines are posted.</p></section>}<section className="matrix-opportunities"><div><p className="score-label text-emerald-300">OPPORTUNITY SCAN</p><strong>{visible.length}-week view · {rows.filter(row=>row.match).length} teams match</strong></div><div className="matrix-candidate-list">{candidates.map(row=><button key={row.code} type="button" onClick={()=>document.getElementById(`team-${row.code}`)?.scrollIntoView({block:'center'})} style={{'--team':colors[row.code]||'#426d85'} as React.CSSProperties}><b>{row.code}</b><span>{row.opportunities} window{row.opportunities===1?'':'s'}</span>{row.cells.map((cell:any,index)=><i key={index} className={cell&&row.matches(cell)?'is-open':''}/>)}</button>)}</div></section><div className="matrix-summary"><strong>Weeks {visible[0]}–{visible.at(-1)}</strong><span>{planCount?`Planned: ${Object.entries(planned).sort(([a],[b])=>Number(a)-Number(b)).map(([week,team])=>`W${week} ${team}`).join(' · ')}`:'Tap a matchup to plan it'}</span>{planCount>0&&<button type="button" onClick={()=>setPlanned({})}>Clear {planCount} plan{planCount===1?'':'s'}</button>}</div><section className="matrix-scroll" aria-label="Survivor planning grid"><table className="matrix-table" style={{minWidth:112+visible.length*66}}><thead><tr><th>TEAM</th>{visible.map(week=><th key={week}>W{week}</th>)}</tr></thead><tbody>{rows.map(row=><tr id={`team-${row.code}`} key={row.code} className={!row.match?'is-dim':''}><th><i style={{background:colors[row.code]||'#3a6175'}}/><span><b>{row.code}</b>{row.isUsed?<small>used</small>:<small>{row.opportunities} open</small>}</span></th>{row.cells.map((cell:any,index)=>{const week=visible[index];const isPlan=planned[week]===row.code;const match=cell&&row.matches(cell);return <td key={week} className={!match?'is-dim':''}>{cell?<button type="button" disabled={row.isUsed} aria-pressed={isPlan} onClick={()=>plan(week,row.code)} className={isPlan?'is-planned':''} style={{borderColor:colors[row.code]||'#3a6175'}}>{isPlan?'✓ ':''}{cell.away?'@ ':''}{cell.opponent}{cell.spread!==null&&<small>{cell.spread>0?'+':''}{cell.spread}</small>}</button>:<span className="matrix-bye">BYE</span>}</td>})}</tr>)}</tbody></table></section><p className="matrix-note">Plans are private drafts on this device and never submit a real Survivor pick. “Open across all entries” helps avoid planning the same team into another life.</p></div>;
}
